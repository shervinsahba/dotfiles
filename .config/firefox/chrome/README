# Create symbolic links from these files to your firefox profile
# which is something like ~/.mozila/firefox/######.default/chrome

cd ~/.mozilla/firefox/*.default/chrome
cp -rs ~/.config/firefox/chrome/* .
